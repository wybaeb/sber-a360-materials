# Шаг 2 · Инструменты анализа и BI-дашборды

**Задание:** сформулировать задачу, которую хотите решить с помощью GigaChat (до Шага 3) —
шаблон `template_gigachat_task.md`.

Материалы встречи:
- `L2.1_sheets_formulas.ipynb` — Excel/Sheets + ИИ: импорт, очистка, агрегация, формулы через ассистента
- `formulas_ai.gs` — формулы через ИИ прямо в Google Sheets (Apps Script; ключи — в Script Properties)
- `synthetic_sales.csv` — учебный датасет продаж (кейсы 2.2–2.4)
- `L4.6_metabase_agent.ipynb` — сборка дашборда через API BI-системы (Metabase; в проде — Навигатор)

Банковские кейсы (синтетические данные в банковском контексте):
- `case_excel_legal_ai/` — Excel-кейс «пилот ИИ для юристов банка»: сырая выгрузка 82 тыс. строк,
  импорт, очистка формулами, сводная «было/стало», решение о пилоте
- `case_bi_contact_center/` — BI-стенд Metabase + PostgreSQL (docker-compose): дашборд речевой
  аналитики контакт-центра — 48 сотрудников, 24 недели. Логин/пароль стенда учебные, локальные
